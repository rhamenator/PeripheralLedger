# Clean-room boundary

Peripheral Ledger is a new implementation based on current public Windows
notification and service APIs. No historical source code, database rows,
organization names, credentials, device identifiers, event records, or user
data are included.

All examples use visibly synthetic paths, IDs, timestamps, and GUIDs. Existing
archives remain read-only.
